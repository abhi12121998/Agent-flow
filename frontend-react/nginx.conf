server {
    listen 80;

    root /usr/share/nginx/html;
    index index.html;

    # SPA fallback — API calls go directly to VITE_API_URL (baked into bundle)
    location / {
        try_files $uri $uri/ /index.html;
    }
}
